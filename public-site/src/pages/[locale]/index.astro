---
import HomePage from '../../components/HomePage.astro'

export function getStaticPaths() {
  return [{ params: { locale: 'en' } }, { params: { locale: 'zh-hant' } }]
}

const locale = Astro.params.locale as 'en' | 'zh-hant'
---
<HomePage locale={locale} />
