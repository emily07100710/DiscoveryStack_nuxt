import { revokeOwnerEntityStrategyProfile, toPublicContentOperationsError } from '../../../../../../content-operations'
import { requireOwner } from '../../../../../../utils/auth'
import { getOwnerDatabaseUserId } from '../../../../../../audit/repository'
export default defineEventHandler(async (event) => { setResponseHeaders(event, { 'cache-control': 'no-store', 'x-robots-tag': 'noindex, nofollow, noarchive' }); try { const owner = await requireOwner(event); const ownerUserId = await getOwnerDatabaseUserId(owner.openId); const clientId = Number(getRouterParam(event, 'id')); const profileId = getRouterParam(event, 'profileId') || ''; if (!Number.isSafeInteger(clientId) || clientId < 1 || !profileId) throw createError({ statusCode: 422, statusMessage: 'Entity Strategy identity is invalid.' }); return revokeOwnerEntityStrategyProfile(ownerUserId, clientId, profileId) } catch (error) { throw toPublicContentOperationsError(error, 'Entity Strategy could not be revoked.') } })
