import { getKnowledgeService, knowledgeValue, readKnowledgeBody, requireKnowledgeOwner, routeError, setKnowledgePrivateApiHeaders, strictKeys } from '../_helpers'
export default defineEventHandler(async event => { setKnowledgePrivateApiHeaders(event); try { const { ownerUserId } = await requireKnowledgeOwner(event); const body = await readKnowledgeBody(event); strictKeys(body, ['claimAId', 'claimBId', 'reason']); const dispute = knowledgeValue(await getKnowledgeService(ownerUserId).createManualDispute(body as never)); return { status: 'success', dispute } } catch (error) { return routeError(error) } })
