import { getRouterParam } from 'h3'
import { getKnowledgeService, knowledgeValue, positiveId, readKnowledgeBody, requireKnowledgeOwner, routeError, setKnowledgePrivateApiHeaders, strictKeys } from '../../_helpers'
export default defineEventHandler(async event => { setKnowledgePrivateApiHeaders(event); try { const { ownerUserId } = await requireKnowledgeOwner(event); const body = await readKnowledgeBody(event); strictKeys(body, ['resolution', 'resolutionNote', 'retractClaimId']); const dispute = knowledgeValue(await getKnowledgeService(ownerUserId).resolveDispute({ disputeId: positiveId(getRouterParam(event, 'id'), 'dispute id'), resolution: body.resolution as never, resolutionNote: body.resolutionNote as string, ...(typeof body.retractClaimId === 'number' ? { retractClaimId: body.retractClaimId } : {}) })); return { status: 'success', dispute } } catch (error) { return routeError(error) } })
