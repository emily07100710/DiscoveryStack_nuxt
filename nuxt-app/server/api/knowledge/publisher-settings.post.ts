import { getKnowledgeService, knowledgeValue, readKnowledgeBody, requireKnowledgeOwner, routeError, setKnowledgePrivateApiHeaders, strictKeys } from './_helpers'
export default defineEventHandler(async event => { setKnowledgePrivateApiHeaders(event); try { const { ownerUserId } = await requireKnowledgeOwner(event); const body = await readKnowledgeBody(event); strictKeys(body, ['organizationEntityId']); const publisherEntity = knowledgeValue(await getKnowledgeService(ownerUserId).setPublisherEntity({ organizationEntityId: body.organizationEntityId as number })); return { status: 'success', publisherEntity } } catch (error) { return routeError(error) } })
