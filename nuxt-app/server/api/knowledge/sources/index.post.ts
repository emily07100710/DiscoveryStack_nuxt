import { getKnowledgeService, knowledgeValue, readKnowledgeBody, requireKnowledgeOwner, routeError, setKnowledgePrivateApiHeaders, strictKeys } from '../_helpers'
export default defineEventHandler(async event => { setKnowledgePrivateApiHeaders(event); try { const { ownerUserId } = await requireKnowledgeOwner(event); const body = await readKnowledgeBody(event); strictKeys(body, ['canonicalUrl', 'title', 'sourceClass', 'notes']); const source = knowledgeValue(await getKnowledgeService(ownerUserId).registerSource(body as never)); return { status: 'success', source } } catch (error) { return routeError(error) } })
