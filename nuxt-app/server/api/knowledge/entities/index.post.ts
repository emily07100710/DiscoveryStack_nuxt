import { getKnowledgeService, knowledgeValue, readKnowledgeBody, requireKnowledgeOwner, routeError, setKnowledgePrivateApiHeaders, strictKeys } from '../_helpers'
export default defineEventHandler(async event => { setKnowledgePrivateApiHeaders(event); try { const { ownerUserId } = await requireKnowledgeOwner(event); const body = await readKnowledgeBody(event); strictKeys(body, ['entityType', 'canonicalName', 'slug', 'canonicalUri', 'locale', 'summary', 'publicVisibility', 'provenance', 'externalIds', 'aliases']); const result = await getKnowledgeService(ownerUserId).createEntity(body as never); return { status: 'success', ...knowledgeValue(result) } } catch (error) { return routeError(error) } })
