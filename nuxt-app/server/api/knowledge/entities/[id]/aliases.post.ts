import { getRouterParam } from 'h3'
import { getKnowledgeService, knowledgeValue, positiveId, readKnowledgeBody, requireKnowledgeOwner, routeError, setKnowledgePrivateApiHeaders, strictKeys } from '../../_helpers'
export default defineEventHandler(async event => { setKnowledgePrivateApiHeaders(event); try { const { ownerUserId } = await requireKnowledgeOwner(event); const body = await readKnowledgeBody(event); strictKeys(body, ['alias', 'locale']); const alias = knowledgeValue(await getKnowledgeService(ownerUserId).addAlias({ entityId: positiveId(getRouterParam(event, 'id'), 'entity id'), alias: body.alias as string, ...(typeof body.locale === 'string' ? { locale: body.locale } : {}) })); return { status: 'success', alias } } catch (error) { return routeError(error) } })
