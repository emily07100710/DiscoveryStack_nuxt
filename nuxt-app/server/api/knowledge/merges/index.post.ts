import { getKnowledgeService, knowledgeValue, readKnowledgeBody, requireKnowledgeOwner, routeError, setKnowledgePrivateApiHeaders, strictKeys } from '../_helpers'
export default defineEventHandler(async event => { setKnowledgePrivateApiHeaders(event); try { const { ownerUserId } = await requireKnowledgeOwner(event); const body = await readKnowledgeBody(event); strictKeys(body, ['sourceEntityId', 'targetEntityId', 'reason', 'candidateId']); const mergeEvent = knowledgeValue(await getKnowledgeService(ownerUserId).mergeEntities(body as never)); return { status: 'success', mergeEvent } } catch (error) { return routeError(error) } })
