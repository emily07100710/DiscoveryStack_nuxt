import { getRouterParam } from 'h3'
import { getKnowledgeService, knowledgeValue, positiveId, readKnowledgeBody, requireKnowledgeOwner, routeError, setKnowledgePrivateApiHeaders, strictKeys } from '../../_helpers'
export default defineEventHandler(async event => { setKnowledgePrivateApiHeaders(event); try { const { ownerUserId } = await requireKnowledgeOwner(event); const body = await readKnowledgeBody(event); strictKeys(body, ['sourceVersionId', 'relation', 'locator', 'contentHash', 'reviewNotes']); const result = knowledgeValue(await getKnowledgeService(ownerUserId).addEvidence({ claimId: positiveId(getRouterParam(event, 'id'), 'claim id'), sourceVersionId: body.sourceVersionId as number, relation: body.relation as never, locator: body.locator as string, contentHash: body.contentHash as string, ...(typeof body.reviewNotes === 'string' ? { reviewNotes: body.reviewNotes } : {}) })); return { status: 'success', ...result } } catch (error) { return routeError(error) } })
