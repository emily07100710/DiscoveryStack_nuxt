import { requireEditorActor } from '../../../../managed-sites/page-editor/http'
import { getDrizzleMediaVaultRepository } from '../../../../managed-sites/media-vault/repository-drizzle'
import { listMediaAssetVersions } from '../../../../managed-sites/media-vault/organization'
export default defineEventHandler(async event => { const { mediaActor } = await requireEditorActor(event); const repository = getDrizzleMediaVaultRepository(); const asset = await repository.findAsset(mediaActor, getRouterParam(event, 'assetId') || ''); if (!asset) throw createError({ statusCode: 404, statusMessage: 'Media asset was not found in this site.' }); const [usageCount, events, versions] = await Promise.all([repository.countActiveUsages(mediaActor, asset.assetId), repository.listEvents(mediaActor, asset.assetId), listMediaAssetVersions(mediaActor, asset.assetId)]); return { asset, usageCount, events, versions } })
