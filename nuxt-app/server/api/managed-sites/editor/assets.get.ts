import { requireEditorActor } from '../../../managed-sites/page-editor/http'
import { getDrizzleMediaVaultRepository } from '../../../managed-sites/media-vault/repository-drizzle'
import { listMediaAssets } from '../../../managed-sites/media-vault/service'
export default defineEventHandler(async event => { const { mediaActor } = await requireEditorActor(event); const query = getQuery(event); const assets = await listMediaAssets(getDrizzleMediaVaultRepository(), mediaActor, { query: typeof query.q === 'string' ? query.q : undefined, status: typeof query.status === 'string' ? query.status : undefined, visibility: typeof query.visibility === 'string' ? query.visibility as any : undefined }); return { assets: assets.map(asset => ({ ...asset, originalObjectKey: undefined, variants: asset.variants.map(({ objectKey: _objectKey, ...variant }) => variant) })) } })
