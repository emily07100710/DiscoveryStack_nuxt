import { proposeAiWebsiteEdit } from '../../../../managed-sites/page-editor/ai'
import { assertEditorSameOrigin, readBoundedEditorBody, requireEditorActor } from '../../../../managed-sites/page-editor/http'
import { getDrizzleMediaVaultRepository } from '../../../../managed-sites/media-vault/repository-drizzle'
import { getDrizzlePageEditorRepository } from '../../../../managed-sites/page-editor/repository-drizzle'
import { createDrizzleAiBudgetPort, saveAiProposal } from '../../../../managed-sites/page-editor/ai-repository-drizzle'
export default defineEventHandler(async event => { assertEditorSameOrigin(event); const { pageActor } = await requireEditorActor(event, 'content:write'); const body = await readBoundedEditorBody(event) as any; const pages = getDrizzlePageEditorRepository(); const page = await pages.findCurrent(pageActor.ownerUserId, pageActor.projectId, String(body.pageId || '')); if (!page) throw createError({ statusCode: 404, statusMessage: 'Page was not found.' }); if (body.expectedPageVersion !== page.version) throw createError({ statusCode: 409, statusMessage: 'AI proposal page version is stale.' }); const media = getDrizzleMediaVaultRepository(); const assets = await media.listAssets(pageActor); const proposal = await proposeAiWebsiteEdit({ actor: pageActor, page, request: body.request, approvedMedia: assets, resolveMedia: (_actor, binding) => media.findAsset(pageActor, binding.assetId), budget: createDrizzleAiBudgetPort(), selectedMediaAssetIds: Array.isArray(body.selectedMediaAssetIds) ? body.selectedMediaAssetIds : [] }); return saveAiProposal(pageActor, proposal) })
