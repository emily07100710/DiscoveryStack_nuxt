import { and, eq, isNull } from 'drizzle-orm'
import { getDatabase } from '../server/database/index.ts'
import { publicIntelligenceArtifacts, publicIntelligenceSources } from '../server/database/schema.ts'
import { createOwnerPublicArtifact, reviewOwnerPublicArtifact } from '../server/public-intelligence/repository.ts'
import { seoGeoMultilabelSchema } from '../server/public-intelligence/seoGeoTaxonomy.ts'

const annotations = [
  {
    structuralArtifactId: 480001,
    sourceUrl: 'https://developers.google.com/search/docs/monitor-debug/debugging-search-traffic-drops?hl=en',
    artifactText: 'Debug organic Search traffic drops by separating algorithmic changes, technical crawl or indexing faults, security and spam issues, seasonality, changing user interests, and site moves. Use Search Console Performance report comparisons to inspect clicks, impressions, queries, URLs, countries, devices, and search appearances. Cross-check Crawl stats, Page indexing, Security issues, Manual Actions, and Google Trends before changing content. Treat small ranking movement differently from persistent large declines, avoid radical changes to already performing pages, and allow enough time to measure the effect of improvements.',
    sourceLocator: 'human-review:batch-09:debugging-search-traffic-drops',
    qualityNote: '人工閱讀官方文件：使用 Performance report、Google Trends、Crawl stats、Page indexing、Security Issues 與 Manual Actions 來鑑別流量下滑模式；涵蓋 algorithmic、technical、security、spam、seasonality、site move 原因、分段比較與避免過度修正。摘要與 reviewed source span 一致，未含 PII。',
    labels: {
      annotationKind: 'seo_geo_multilabel', annotationVersion: 'seo-geo-journey-v1', primaryJourneyStage: 'response', journeyStages: ['understanding', 'response', 'progression'], searchIntents: ['informational'], contentTypes: ['editorial', 'tool'], audienceRoles: ['practitioner', 'technical_evaluator', 'decision_maker'], topicClusters: ['organic traffic drops', 'Search Console Performance report', 'technical SEO diagnosis', 'ranking fluctuation', 'seasonality analysis', 'Google Trends'], entitySignals: [{ name: 'Search Console Performance report', type: 'service', relationship: 'Supports segmented analysis of clicks, impressions, queries, URLs, countries, devices, and search appearances.' }, { name: 'Google Trends', type: 'service', relationship: 'Helps distinguish a site-specific traffic decline from broader demand and seasonality changes.' }, { name: 'Google Search', type: 'organisation', relationship: 'May change ranking and serving systems while continuously evaluating web content.' }], geoSignals: ['global', 'country'], citationReadiness: ['first_party_expertise', 'dated_or_current', 'source_links'], technicalSeoSignals: ['title_present', 'h1_present', 'indexable', 'internal_routing', 'performance_not_observed'], frictionSignals: ['information_overload'], actionPriority: 'high', annotationRationale: '本頁將流量下滑轉為可重現的診斷與回應流程，結合多個第一方報告及時間、地域、裝置與搜尋外觀切片，適合訓練 response-stage 的 SEO/GEO 問題歸因、摩擦辨識與低風險行動優先順序。', reviewerConfidence: 5,
    },
  },
  {
    structuralArtifactId: 480002,
    sourceUrl: 'https://developers.google.com/search/docs/crawling-indexing/site-move-with-url-changes?hl=en',
    artifactText: 'Move a website with URL changes through a planned mapping from old URLs to the most relevant new destinations. Test the new site, use permanent server redirects, preserve crawl access, remove temporary robots or noindex blocks at launch, submit sitemaps, and verify old and new properties in Search Console. Sequence large changes, choose lower-traffic periods where practical, monitor old and new URLs, logs, and indexing during recrawl, and expect temporary ranking fluctuation while Google processes moves per URL. Keep HTTPS certificates, server capacity, canonical details, and content assets ready before migration.',
    sourceLocator: 'human-review:batch-09:site-move-with-url-changes',
    qualityNote: '人工閱讀官方文件：涵蓋 staged migration、old/new URL mapping、permanent redirects、HTTPS/TLS、robots/noindex cleanup、Search Console verification、sitemap、server capacity、per-URL recrawl 與 post-move monitoring。摘要與 reviewed source span 一致，未含 PII。',
    labels: {
      annotationKind: 'seo_geo_multilabel', annotationVersion: 'seo-geo-journey-v1', primaryJourneyStage: 'progression', journeyStages: ['response', 'progression', 'conversion'], searchIntents: ['informational', 'transactional'], contentTypes: ['service', 'tool', 'editorial'], audienceRoles: ['decision_maker', 'practitioner', 'technical_evaluator'], topicClusters: ['site migration', 'URL mapping', 'permanent redirects', 'Search Console verification', 'sitemap submission', 'HTTPS migration'], entitySignals: [{ name: 'Googlebot', type: 'service', relationship: 'Recrawls old and new URLs as a site move is processed per URL.' }, { name: 'Search Console', type: 'service', relationship: 'Verifies properties and exposes migration-related indexing and sitemap signals.' }, { name: '301 redirect', type: 'concept', relationship: 'Provides a permanent redirect from an old URL to its mapped destination.' }], geoSignals: ['global', 'not_applicable'], citationReadiness: ['first_party_expertise', 'dated_or_current', 'source_links'], technicalSeoSignals: ['canonical_present', 'indexable', 'internal_routing', 'language_signal', 'performance_not_observed'], frictionSignals: ['missing_next_step', 'information_overload'], actionPriority: 'critical', annotationRationale: '本頁將 URL-changing migration 的使用者可見度風險轉成前置測試、映射、redirect、可索引性、觀測與分段移轉行動，適合訓練 progression-stage 的技術 SEO 交付決策及轉換風險控制。', reviewerConfidence: 5,
    },
  },
  {
    structuralArtifactId: 480003,
    sourceUrl: 'https://developers.google.com/search/docs/crawling-indexing/site-move-no-url-changes?hl=en',
    artifactText: 'Change a hosting provider or CDN without changing user-visible URLs by copying and testing the site, checking Googlebot access, reviewing Search Console verification, lowering DNS TTL in advance, and removing temporary crawl or index blocks at launch. Switch DNS only after readiness checks, monitor server logs, public DNS propagation, crawling, and traffic on old and new infrastructure, then shut down the old host only after users and Googlebot receive the correct content. Temporary crawl-rate fluctuation can occur after the change and should be interpreted with infrastructure monitoring rather than guessed from rankings alone.',
    sourceLocator: 'human-review:batch-09:site-move-no-url-changes',
    qualityNote: '人工閱讀官方文件：涵蓋 hosting/CDN/DNS migration、test environment、Googlebot access、Search Console verification、TTL、temporary noindex/robots cleanup、server-log/DNS/crawl monitoring、old-host shutdown 與 crawl-rate fluctuation。摘要與 reviewed source span 一致，未含 PII。',
    labels: {
      annotationKind: 'seo_geo_multilabel', annotationVersion: 'seo-geo-journey-v1', primaryJourneyStage: 'progression', journeyStages: ['response', 'progression'], searchIntents: ['informational', 'transactional'], contentTypes: ['service', 'tool', 'editorial'], audienceRoles: ['decision_maker', 'practitioner', 'technical_evaluator'], topicClusters: ['hosting migration', 'CDN migration', 'DNS TTL', 'Googlebot access', 'crawl monitoring', 'Search Console verification'], entitySignals: [{ name: 'Googlebot', type: 'service', relationship: 'Needs to access the new hosting infrastructure during a DNS or CDN migration.' }, { name: 'DNS', type: 'concept', relationship: 'Routes a domain to the new infrastructure after records propagate.' }, { name: 'Search Console', type: 'service', relationship: 'Helps retain ownership verification and monitor indexing during infrastructure change.' }], geoSignals: ['global', 'not_applicable'], citationReadiness: ['first_party_expertise', 'dated_or_current', 'source_links'], technicalSeoSignals: ['indexable', 'internal_routing', 'language_signal', 'performance_not_observed'], frictionSignals: ['missing_next_step', 'information_overload'], actionPriority: 'high', annotationRationale: '本頁把不改 URL 的基礎設施變更連結到可觀測的 DNS、crawlability、indexability 與流量切換流程，適合訓練 progression-stage 的網站營運、技術風險與驗收洞察。', reviewerConfidence: 5,
    },
  },
  {
    structuralArtifactId: 480004,
    sourceUrl: 'https://developers.google.com/search/docs/crawling-indexing/mobile/mobile-sites-mobile-first-indexing?hl=en',
    artifactText: 'Mobile-first indexing uses the mobile version of site content for indexing and ranking. Prefer responsive design when possible; if using dynamic serving or separate mobile URLs, keep primary content, headings, robots directives, metadata, structured data, image alt text, and video information equivalent across desktop and mobile. Let Google render resources, avoid interaction-gated primary lazy-loaded content, validate mobile URLs and redirects, align hreflang for separate URLs, and troubleshoot mobile noindex, blocked resources, error pages, duplicate targets, missing media, and quality gaps that can reduce search visibility.',
    sourceLocator: 'human-review:batch-09:mobile-first-indexing',
    qualityNote: '人工閱讀官方文件：涵蓋 smartphone-agent mobile-first indexing、responsive/dynamic/separate URL architecture、renderability、content/heading/metadata/structured-data parity、media/ads、hreflang 及 mobile-specific troubleshooting。摘要與 reviewed source span 一致，未含 PII。',
    labels: {
      annotationKind: 'seo_geo_multilabel', annotationVersion: 'seo-geo-journey-v1', primaryJourneyStage: 'progression', journeyStages: ['understanding', 'response', 'progression', 'conversion'], searchIntents: ['informational', 'commercial'], contentTypes: ['service', 'editorial'], audienceRoles: ['buyer', 'practitioner', 'technical_evaluator', 'local_visitor'], topicClusters: ['mobile-first indexing', 'responsive design', 'mobile content parity', 'structured data parity', 'mobile rendering', 'hreflang'], entitySignals: [{ name: 'Google Search', type: 'organisation', relationship: 'Uses the mobile version of site content for indexing and ranking.' }, { name: 'Googlebot smartphone', type: 'service', relationship: 'Fetches and renders mobile content and resources for mobile-first indexing.' }, { name: 'responsive design', type: 'concept', relationship: 'Serves the same HTML at the same URL while adapting presentation to screen size.' }], geoSignals: ['global', 'multilingual', 'city_or_local'], citationReadiness: ['first_party_expertise', 'dated_or_current', 'source_links', 'structured_data'], technicalSeoSignals: ['title_present', 'h1_present', 'canonical_present', 'indexable', 'structured_data', 'internal_routing', 'language_signal'], frictionSignals: ['missing_next_step', 'information_overload'], actionPriority: 'high', annotationRationale: '本頁連結行動裝置使用者體驗、renderability、內容與 metadata parity、structured data、local/mobile visitor 與 international routing，適合訓練 progression-stage 的 SEO/GEO 可見度、技術摩擦與行動優先度判斷。', reviewerConfidence: 5,
    },
  },
]

const database = getDatabase()
if (!database) throw new Error('database_unavailable')
const [source] = await database.select({ id: publicIntelligenceSources.id, ownerUserId: publicIntelligenceSources.ownerUserId, retentionUntil: publicIntelligenceSources.retentionUntil }).from(publicIntelligenceSources).where(and(
  eq(publicIntelligenceSources.sourceName, 'Google Search Central Documentation（CC BY 4.0）'), eq(publicIntelligenceSources.reviewStatus, 'approved'), eq(publicIntelligenceSources.allowedUse, 'training_candidate'), eq(publicIntelligenceSources.robotsStatus, 'reviewed_allow'), eq(publicIntelligenceSources.termsStatus, 'allows_training'), eq(publicIntelligenceSources.copyrightRisk, 'low'), eq(publicIntelligenceSources.piiStatus, 'none_detected'), isNull(publicIntelligenceSources.removedAt),
)).limit(1)
if (!source) throw new Error('approved_google_search_central_source_not_found')

for (const annotation of annotations) {
  const [structural] = await database.select({ id: publicIntelligenceArtifacts.id, sourceSpanHash: publicIntelligenceArtifacts.sourceSpanHash, qualityStatus: publicIntelligenceArtifacts.qualityStatus, piiStatus: publicIntelligenceArtifacts.piiStatus }).from(publicIntelligenceArtifacts).where(and(eq(publicIntelligenceArtifacts.id, annotation.structuralArtifactId), eq(publicIntelligenceArtifacts.sourceId, source.id), eq(publicIntelligenceArtifacts.artifactType, 'structural_features'), eq(publicIntelligenceArtifacts.sourceUrl, annotation.sourceUrl), isNull(publicIntelligenceArtifacts.removedAt))).limit(1)
  if (!structural?.sourceSpanHash) throw new Error(`batch_09_structural_artifact_not_found:${annotation.structuralArtifactId}`)
  if (structural.piiStatus !== 'none_detected') throw new Error(`batch_09_pii_not_clear:${annotation.structuralArtifactId}`)
  if (structural.qualityStatus !== 'passed') await reviewOwnerPublicArtifact({ ownerUserId: source.ownerUserId, artifactId: structural.id, qualityStatus: 'passed', qualityNote: annotation.qualityNote })
  const labels = seoGeoMultilabelSchema.parse(annotation.labels)
  const [existing] = await database.select({ id: publicIntelligenceArtifacts.id, qualityStatus: publicIntelligenceArtifacts.qualityStatus }).from(publicIntelligenceArtifacts).where(and(eq(publicIntelligenceArtifacts.sourceId, source.id), eq(publicIntelligenceArtifacts.artifactType, 'human_annotation'), eq(publicIntelligenceArtifacts.sourceUrl, annotation.sourceUrl), eq(publicIntelligenceArtifacts.sourceSpanHash, structural.sourceSpanHash), isNull(publicIntelligenceArtifacts.removedAt))).limit(1)
  const human = existing || await createOwnerPublicArtifact({ ownerUserId: source.ownerUserId, sourceId: source.id, sourceUrl: annotation.sourceUrl, canonicalUrl: annotation.sourceUrl, artifactType: 'human_annotation', artifactText: annotation.artifactText, sourceLocator: annotation.sourceLocator, sourceSpanHash: structural.sourceSpanHash, fieldData: labels, language: 'en', extractionMethod: 'human_annotation', requestedUse: 'training_candidate', retentionUntil: source.retentionUntil })
  if (human.qualityStatus !== 'passed') await reviewOwnerPublicArtifact({ ownerUserId: source.ownerUserId, artifactId: human.id, qualityStatus: 'passed', qualityNote: annotation.qualityNote })
  console.log(JSON.stringify({ sourceUrl: annotation.sourceUrl, structuralArtifactId: structural.id, humanAnnotationId: human.id, primaryJourneyStage: labels.primaryJourneyStage, status: 'passed' }))
}
