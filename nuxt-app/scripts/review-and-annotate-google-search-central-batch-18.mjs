import { and, eq, isNull } from 'drizzle-orm'
import { getDatabase } from '../server/database/index.ts'
import { publicIntelligenceArtifacts, publicIntelligenceSources } from '../server/database/schema.ts'
import { createOwnerPublicArtifact, reviewOwnerPublicArtifact } from '../server/public-intelligence/repository.ts'
import { seoGeoMultilabelSchema } from '../server/public-intelligence/seoGeoTaxonomy.ts'

const annotations = [
  {
    structuralArtifactId: 960001,
    sourceUrl: 'https://developers.google.com/search/docs/monitor-debug/search-console-start?hl=en',
    sourceLocator: 'human-review:batch-18:search-console-start',
    artifactText: 'Use Search Console to turn a visibility question into an ordered response. First verify site ownership, then check whether Google can find and read pages by reviewing indexing errors, warnings, and exclusions. Use sitemaps to communicate site URLs and monitor their processing, but do not assume a sitemap replaces crawlable pages. Examine performance by queries, pages, and countries to identify a traffic change and prioritize investigation. Choose the report that matches the observed problem: manual actions for policy issues, removals for temporary hiding, migration guidance for domain changes, rich-result status for readable structured data, URL inspection for a page-level index question, security issues for harmful behavior, and Core Web Vitals for field page-experience signals. The useful response is evidence-led and scoped to the failure mode rather than a generic request for more traffic.',
    qualityNote: '人工閱讀官方 Search Console 入門文件：涵蓋 ownership verification、Index Coverage、sitemap、Performance report、manual actions、removals、migration、rich-result status、URL Inspection、security issues 與 Core Web Vitals。去識別摘要與 reviewed source span 一致，未含 PII。',
    labels: {
      annotationKind: 'seo_geo_multilabel', annotationVersion: 'seo-geo-journey-v1', primaryJourneyStage: 'response', journeyStages: ['discovery', 'progression', 'response'], searchIntents: ['informational'], contentTypes: ['tool', 'editorial'], audienceRoles: ['practitioner', 'technical_evaluator', 'decision_maker', 'researcher'], topicClusters: ['Search Console diagnosis', 'indexing issue triage', 'search performance analysis', 'structured data debugging', 'site migration response', 'page experience monitoring'], entitySignals: [{ name: 'Google Search Console', type: 'service', relationship: 'Surfaces reports for investigating how Google crawls, indexes, and serves a website.' }, { name: 'URL Inspection', type: 'service', relationship: 'Supports a page-level response to an observed indexing question.' }, { name: 'Search performance report', type: 'service', relationship: 'Breaks down visibility by queries, pages, and countries to inform prioritization.' }], geoSignals: ['global', 'country', 'not_applicable'], citationReadiness: ['first_party_expertise', 'dated_or_current', 'source_links'], technicalSeoSignals: ['title_present', 'h1_present', 'canonical_present', 'indexable', 'structured_data', 'performance_not_observed'], frictionSignals: ['information_overload', 'missing_next_step', 'unclear_value'], actionPriority: 'high', annotationRationale: '本頁提供將 crawl、index、appearance 與 performance 訊號轉換為分流處置的首方工具框架，核心屬於 response，而非聲稱單一報表能保證排名或流量。', reviewerConfidence: 5,
    },
  },
  {
    structuralArtifactId: 960002,
    sourceUrl: 'https://developers.google.com/search/docs/crawling-indexing/ask-google-to-recrawl?hl=en',
    sourceLocator: 'human-review:batch-18:ask-google-to-recrawl',
    artifactText: 'After publishing or materially changing a page, choose a re-indexing response that matches the number of managed URLs. For a small set of pages, inspect a specific URL and request a crawl only when the requester has the required property access. Respect the request quota and do not repeat a request expecting it to accelerate processing. For many URLs, use a sitemap to communicate discoverable URLs, including during a site launch or move. Set expectations clearly: crawling can take days or weeks, and a request neither guarantees immediate crawling nor inclusion in search results. Monitor index status or URL inspection evidence while the system evaluates the updated page, and focus on making the content useful and technically accessible instead of treating submission as a ranking command.',
    qualityNote: '人工閱讀官方 recrawl 文件：涵蓋受管理 URL 的 re-index request、等待期、不保證收錄、URL Inspection owner/full-user 前提與 quota、large-URL sitemap、launch/site move 及 Index Status monitoring。去識別摘要與 reviewed source span 一致，未含 PII。',
    labels: {
      annotationKind: 'seo_geo_multilabel', annotationVersion: 'seo-geo-journey-v1', primaryJourneyStage: 'response', journeyStages: ['progression', 'response'], searchIntents: ['informational'], contentTypes: ['tool', 'editorial'], audienceRoles: ['practitioner', 'technical_evaluator', 'decision_maker'], topicClusters: ['re-indexing response', 'URL inspection', 'sitemap submission', 'crawl request quota', 'site launch', 'index status monitoring'], entitySignals: [{ name: 'Google Search', type: 'service', relationship: 'Evaluates crawl and inclusion requests without guaranteeing immediate search-result inclusion.' }, { name: 'URL Inspection', type: 'service', relationship: 'Provides a small-set URL pathway for inspecting and requesting crawl of managed pages.' }, { name: 'Sitemap', type: 'concept', relationship: 'Communicates many URLs and can support discovery after a launch or site move.' }], geoSignals: ['global', 'not_applicable'], citationReadiness: ['first_party_expertise', 'dated_or_current', 'source_links'], technicalSeoSignals: ['title_present', 'h1_present', 'canonical_present', 'indexable', 'performance_not_observed'], frictionSignals: ['unclear_value', 'missing_next_step'], actionPriority: 'medium', annotationRationale: '本頁界定更新後 URL 的可執行 re-indexing response、等待與監測方式，並明確避免將 crawl request 誤解為保證收錄或即時排名改善。', reviewerConfidence: 5,
    },
  },
]

const database = getDatabase()
if (!database) throw new Error('database_unavailable')
const [source] = await database.select({ id: publicIntelligenceSources.id, ownerUserId: publicIntelligenceSources.ownerUserId, retentionUntil: publicIntelligenceSources.retentionUntil }).from(publicIntelligenceSources).where(and(eq(publicIntelligenceSources.sourceName, 'Google Search Central Documentation（CC BY 4.0）'), eq(publicIntelligenceSources.reviewStatus, 'approved'), eq(publicIntelligenceSources.allowedUse, 'training_candidate'), eq(publicIntelligenceSources.robotsStatus, 'reviewed_allow'), eq(publicIntelligenceSources.termsStatus, 'allows_training'), eq(publicIntelligenceSources.copyrightRisk, 'low'), eq(publicIntelligenceSources.piiStatus, 'none_detected'), isNull(publicIntelligenceSources.removedAt))).limit(1)
if (!source) throw new Error('approved_google_search_central_source_not_found')

for (const annotation of annotations) {
  const [structural] = await database.select({ id: publicIntelligenceArtifacts.id, sourceSpanHash: publicIntelligenceArtifacts.sourceSpanHash, qualityStatus: publicIntelligenceArtifacts.qualityStatus, piiStatus: publicIntelligenceArtifacts.piiStatus }).from(publicIntelligenceArtifacts).where(and(eq(publicIntelligenceArtifacts.id, annotation.structuralArtifactId), eq(publicIntelligenceArtifacts.sourceId, source.id), eq(publicIntelligenceArtifacts.artifactType, 'structural_features'), eq(publicIntelligenceArtifacts.sourceUrl, annotation.sourceUrl), isNull(publicIntelligenceArtifacts.removedAt))).limit(1)
  if (!structural?.sourceSpanHash) throw new Error(`batch_18_structural_artifact_not_found:${annotation.structuralArtifactId}`)
  if (structural.piiStatus !== 'none_detected') throw new Error(`batch_18_pii_not_clear:${annotation.structuralArtifactId}`)
  if (structural.qualityStatus !== 'passed') await reviewOwnerPublicArtifact({ ownerUserId: source.ownerUserId, artifactId: structural.id, qualityStatus: 'passed', qualityNote: annotation.qualityNote })
  const labels = seoGeoMultilabelSchema.parse(annotation.labels)
  const [existing] = await database.select({ id: publicIntelligenceArtifacts.id, qualityStatus: publicIntelligenceArtifacts.qualityStatus }).from(publicIntelligenceArtifacts).where(and(eq(publicIntelligenceArtifacts.sourceId, source.id), eq(publicIntelligenceArtifacts.artifactType, 'human_annotation'), eq(publicIntelligenceArtifacts.sourceUrl, annotation.sourceUrl), eq(publicIntelligenceArtifacts.sourceSpanHash, structural.sourceSpanHash), isNull(publicIntelligenceArtifacts.removedAt))).limit(1)
  const human = existing || await createOwnerPublicArtifact({ ownerUserId: source.ownerUserId, sourceId: source.id, sourceUrl: annotation.sourceUrl, canonicalUrl: annotation.sourceUrl, artifactType: 'human_annotation', artifactText: annotation.artifactText, sourceLocator: annotation.sourceLocator, sourceSpanHash: structural.sourceSpanHash, fieldData: labels, language: 'en', extractionMethod: 'human_annotation', requestedUse: 'training_candidate', retentionUntil: source.retentionUntil })
  if (human.qualityStatus !== 'passed') await reviewOwnerPublicArtifact({ ownerUserId: source.ownerUserId, artifactId: human.id, qualityStatus: 'passed', qualityNote: annotation.qualityNote })
  console.log(JSON.stringify({ sourceUrl: annotation.sourceUrl, structuralArtifactId: structural.id, humanAnnotationId: human.id, primaryJourneyStage: labels.primaryJourneyStage, status: 'passed' }))
}
